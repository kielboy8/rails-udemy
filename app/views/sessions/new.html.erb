<h1 class="display-5 text-center font-weight-normal">Login</h1>

<!--  form_for(@article, :html => {class: "form-horizontal", role: "form"} do |f| -->
<%= form_for(:session, :html => {role: "form"}, url: login_path) do |f| %>
<div class="form-group row mt-4">
    <div class="col-sm-2 col-form-label">
        <%= f.label :email, "Email", class: "float-right font-weight-bold" %>
    </div>
    <div class="col-sm">
        <%= f.email_field :email, class: "form-control", placeholder: "Enter Email" %>
    </div>
</div>
<div class="form-group row mt-4">
    <div class="col-sm-2 col-form-label">
        <%= f.label :password, "Password", class: "float-right font-weight-bold"  %>
    </div>
    <div class="col-sm">
        <%= f.password_field :password, class: "form-control", placeholder: "Enter password", automcomplete: "off" %>
    </div>
</div>
<div class="form-group row mt-4">
    <div class="col-sm-2 offset-sm-2">
        <%= f.submit "Login", class: "btn btn-primary" %>
    </div>
</div>
<% end %>